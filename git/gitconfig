# --- Identity ---
[include]
    path = ~/.gitconfig.local

# --- Core ---
[core]
    pager = delta

# --- Delta ---
[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    side-by-side = true
    line-numbers = true
