set -g mouse on

# 256색 지원
set -g default-terminal "tmux-256color"

# ESC 지연 제거
set -sg escape-time 0

# 스크롤백 버퍼 확대
set -g history-limit 50000

# 패널/윈도우 번호 1부터 시작
set -g base-index 1
setw -g pane-base-index 1

# 동시 입력 토글 (Ctrl+B → Shift+S)
bind-key S set-window-option synchronize-panes

